#include<stdio.h>
int main()
{
    printf("value of the int : %zu bytes\n",sizeof(int));
    printf("value of the char : %zu bytes\n",sizeof(char));
    printf("value of the float : %zu bytes\n",sizeof(float));
    return 0;
}